1. d = {}
2. d = {'foo':42}
3. Dictionary is unordered and contains pairs.
4. There is no key 'foo' so you get a keyerror.
5. 'cat' in spam will test if 'cat' is a key or a value. 'cat' in spam.keys() only tests if it is a key
6. same as above except just looks in values
7. spam.setdefault('color', 'black')
8. import pprint
pprint.pprint(string)
